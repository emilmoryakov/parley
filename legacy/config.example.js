/*
 * Parley — configuration template.
 *
 * Copy this file to `config.js` and drop in your own OpenRouter key:
 *   Windows : copy config.example.js config.js
 *   macOS/Linux : cp config.example.js config.js
 *
 * `config.js` is gitignored, so your key never gets committed. Grab a key at
 * https://openrouter.ai/keys and browse model ids at https://openrouter.ai/models.
 */

export const OPENROUTER_API_KEY = "YOUR_OPENROUTER_API_KEY";
export const MODEL = "openai/gpt-4o-mini";
